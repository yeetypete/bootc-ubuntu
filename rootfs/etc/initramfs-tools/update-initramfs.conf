# Disable initramfs regeneration on package installs. dracut ships an
# initramfs-tools compatible update-initramfs and a dpkg trigger on it, and the
# image builds its initramfs in the rootfs stage instead.
update_initramfs=no
