# Symlink /var/lib/dpkg to its relocated path in /usr/lib/sysimage, where the dpkg
# database persists across image updates (/var is only applied at first provisioning).
L+ /var/lib/dpkg - - - - /usr/lib/sysimage/dpkg
