# Groups created by package postinst scripts that ship no sysusers.d of their own
# (_ssh from openssh-server, netdev from network-manager). Declaring them keeps
# their GIDs stable: /etc is persistent, so a locally modified /etc/group stops
# taking image updates.
g _ssh -
g netdev -
