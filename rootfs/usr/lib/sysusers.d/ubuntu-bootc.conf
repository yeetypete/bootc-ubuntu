# Groups created by package postinst scripts that ship no sysusers.d file of
# their own. Declaring them here keeps their GIDs stable across rebuilds, which
# matters because /etc is persistent: once /etc/group is modified locally it
# stops taking updates from the image.
#
# _ssh comes from openssh-server, netdev from network-manager.
g _ssh -
g netdev -
