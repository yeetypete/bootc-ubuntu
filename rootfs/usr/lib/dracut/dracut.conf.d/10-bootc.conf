# dracut 110 does not infer these, and without them the bootc module fails to
# install its unit: dracut still exits 0, but the initramfs silently ends up
# without bootc-root-setup.service and the system cannot mount its root.
systemdsystemconfdir=/etc/systemd/system
systemdsystemunitdir=/usr/lib/systemd/system

# The bootc dracut module never auto-installs and must be requested. It sets up
# /sysroot as a composefs deployment via bootc-root-setup.service and pulls in
# the erofs + overlay kernel modules.
add_dracutmodules+=" bootc "
