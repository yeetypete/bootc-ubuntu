# Stop package installs regenerating the initramfs. dracut ships an
# initramfs-tools compatible update-initramfs with a dpkg trigger on it, and
# bootc-ubuntu-imagectl builds the real one.
update_initramfs=no
