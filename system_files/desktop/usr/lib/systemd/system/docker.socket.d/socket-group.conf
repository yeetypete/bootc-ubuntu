# GNOME Initial Setup makes the first account an administrator, which on Ubuntu
# means membership of the sudo group. Let that group use the rootful daemon's
# socket directly, so no user has to be added to the docker group afterwards.
[Socket]
SocketGroup=sudo
